# Web server port (default 3001 if 3000 is in use)
PORT=3001

# FastAPI backend (server-side proxy uses API_URL; client uses NEXT_PUBLIC_*)
API_URL=http://localhost:8000
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_API_KEY=dev-api-key-change-me
